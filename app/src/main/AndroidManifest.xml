<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muhammadsalsabiluas2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuhammadSalsabilUAS2021"
        android:usesCleartextTraffic="true">
        <activity android:name=".AllActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AllActivity.LoginActivity" />
        <activity android:name=".AllActivity.HomePage" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".UpdateUserInfoActivity" />
    </application>

</manifest>